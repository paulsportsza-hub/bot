# MzansiEdge Health Dashboard — env vars
# Copy to ../.env (the main bot .env) and set real values

# Dashboard auth (HTTP Basic)
DASHBOARD_USER=admin
DASHBOARD_PASS=mzansiedge

# Port (default 8501 — bot uses 8443)
DASHBOARD_PORT=8501
