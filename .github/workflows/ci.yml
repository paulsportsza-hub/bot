# DISABLED 2026-04-18 — launch priority.
# Reason: tests depend on server-only layout (/home/paulsportsza/ paths,
# orphan contracts/ package, in-place scripts). CI runner cannot reproduce
# the environment without structural work. Server-side pre_merge_gate.sh
# runs on every push to main and is the canonical gate until post-launch.
#
# To re-enable: restore the on: block below and fix the CI environment
# to match server layout (vendor contracts package, add playwright +
# jinja2 already added, resolve sys.path hacks in tests/contracts/*).

name: Pre-Merge Gate (disabled)
on:
  workflow_dispatch:

jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'CI disabled — see comment at top of ci.yml'
